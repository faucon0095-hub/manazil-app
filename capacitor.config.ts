import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.senastro.manazil',
  appName: 'Manazil Sen-Astro',
  webDir: 'build'
};

export default config;
